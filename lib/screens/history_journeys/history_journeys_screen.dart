import 'package:flutter/material.dart';

class HistoryJourneysScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('History Journeys'),
      ),
      body: Center(
        child: Text('History journeys'),
      ),
    );
  }
}