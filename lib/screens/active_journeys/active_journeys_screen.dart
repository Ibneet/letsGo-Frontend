import 'package:flutter/material.dart';

class ActiveJourneysScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Active Journeys'),
      ),
      body: Center(
        child: Text('Active journeys'),
      ),
    );
  }
}